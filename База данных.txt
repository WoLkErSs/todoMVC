CREATE TABLE `tasks`(id MEDIUMINT AUTO_INCREMENT PRIMARY KEY,`order_tasks` MEDIUMINT UNIQUE NOT NULL ,task VARCHAR(255), status BOOLEAN DEFAULT false, todo_id MEDIUMINT NOT NULL, FOREIGN KEY (todo_id) REFERENCES to_do_lists(id) ON UPDATE CASCADE ON DELETE CASCADE);

